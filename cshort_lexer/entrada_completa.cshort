int a;
float b[10];
char c;

int soma(int x, int y) {
    int resultado;
    resultado = x + y;
    return resultado;
}

void main() {
    int i;
    for (i = 0; i < 10; i = i + 1)
        soma(a, b[i]);

    if (a > 0)
        c = 'x';
    else
        c = 'y';
}
